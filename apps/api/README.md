# TradeFlow AI API
#
# Install:  pip install -e ".[dev]"
# Run:      uvicorn tradeflow.main:app --reload --app-dir src
# Test:     pytest
# Lint:     ruff check src tests && ruff format --check src tests
# Migrate:  alembic upgrade head
